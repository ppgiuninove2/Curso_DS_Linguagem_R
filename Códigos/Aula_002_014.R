#Sintaxe: locator(n) #localiza n pontos
#Exemplo:
x=1:20
y=sqrt(x)
plot(x,y)
text(locator(1),"ponto")
#onde for dado o clique será escrita a mensagem
#ou de outra forma:
plot(x,y)
locator(2)
#localiza dois pontos e dá suas coordenadas na console
x
